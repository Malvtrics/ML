import math  #开始的时候import math as m 但是原文已经有了一个全局变量m 导致程序无法运行成功 ，所以起别名的时候最好有意义，math 已经很简单明了不用再起名
def prime(n):
      if n<4:
            return True
      else:
            for i in range(2,int(math.sqrt(n))+1): 
                  if n%i == 0:
                        return False
            else:
                  return True

arr = []
m = 0
n = eval(input())
if int(n) != n:
      m = int(n) + 1
else:
      m = n

while len(arr) < 5:
      if prime(m):
            arr.append(str(m))
      m += 1

print(','.join(arr))
