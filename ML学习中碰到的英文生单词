Normal words:

negligable -> so small or unimportant as to be not worth considering; insignificant.
interpolate -> 插值
calibration -> 校准
monotonically -> 单调
revamp -> 改造
myriad -> 无数的
aspect ratio -> 图像的宽高比
flir forward looking infra-red -> 前视红外雷达
Euclidean space ->　欧式空间
harmonic embeddings -> 谐波嵌入
off-the-shelf -> 现成的
bottleneck layer -> 瓶颈层 -> 1*1卷积
coordinate -> 坐标
convergence -> 收敛
spatial -> 空间的
compactness -> 紧凑
proximity -> 接近
elbow -> 弯头 肘
meta algrithom -> Loosely speaking, a meta-algorithm is an algorithm that wraps and executes other algorithms 
    and might feed them input data or use their output data. A common goal is to achieve a better task performance 
    compared to the performance of each of those algorithms on their own.
Springs show the spatial connections between parts
chronological -> 按照时间顺序的
SOTA -> state-of-the-art -> 目前最好的
cascade -> 级联的
occlusion -> 咬合
deformable -> 变形的
if certain joints(关节) are hidden, they can be estimated if the pose is reasoned(推理) about holistically(整体)
 
Abbreviations:

CUDA -> compute unified device architecture->NVIDIA 推出的通用并行计算架构，使GPU可以解决复杂的计算问题
NVIDIA -> 因为invidia被人注册改成了nvidia 所以中文翻译是英伟达而不是恩伟达 拉丁文中，Invidia有忌妒与目不转睛之意-》“邪恶之眼”
PASCAL VOC Challenge->提供数据集的，在计算视觉的领域中，Pascal VOC Challenge 就好比是数学中的哥德巴赫猜想一样。
  Pascal的全称是Pattern Analysis, Statical Modeling and Computational Learning。每年，该组织都会提供一系列类别的、带标签的图片，
  挑战者通过设计各种精妙的算法，仅根据分析图片内容来将其分类，最终通过准确率、召回率、效率来一决高下
ILSVRC -> imagenet large scale visual recognition chanllenge -> 
  evluate algrithms for object detection and image classification at large scale
CLVR -> cross level vehicle recongnition 
